@{
    string CurrentControllerName = Convert.ToString(((Microsoft.AspNetCore.Mvc.Controllers.ControllerActionDescriptor)ViewContext.ActionDescriptor).ControllerName);  
    switch (CurrentControllerName)
    {
        case "Admin":
            Layout = "_AdminLayout";
            break;
        default:
            Layout = "_Layout";
            break;
    }
}
